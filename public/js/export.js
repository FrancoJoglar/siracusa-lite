// ═══ EXPORT ═══
async function doExport(){const f=document.getElementById('exp-fecha').value;if(!f)return;const data=await api('/api/solicitudes?fecha='+f);const p=document.getElementById('exp-prev'),t=document.getElementById('exp-tbl'),c=document.getElementById('exp-cnt');if(!data.length){t.innerHTML='<p class="text-gray-500">Sin datos</p>';p.classList.remove('hidden');return;}let h='<table class="w-full"><tr class="text-gray-500"><th class="text-left pr-4">Eq</th><th class="text-left pr-4">Sec</th><th class="text-right pr-4">Hrs</th><th class="text-right pr-4">M³</th><th class="text-right">Fert</th></tr>';data.forEach(r=>{const ft=FK.reduce((a,k)=>a+(r[k]||0),0);h+=`<tr><td class="pr-4">${r.equipo_name}</td><td class="pr-4">${r.sector_name}</td><td class="text-right pr-4 font-semibold">${r.horas}</td><td class="text-right pr-4 text-blue-600">${(r.m3_programados||0).toFixed(1)}</td><td class="text-right">${ft.toFixed(1)}</td></tr>`;});h+='</table>';t.innerHTML=h;c.textContent=`${data.length} solicitudes`;p.classList.remove('hidden');window.location.href='/api/export?fecha='+f;}
